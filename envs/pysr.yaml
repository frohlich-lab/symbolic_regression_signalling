name: pysr_env
channels:
  - conda-forge
dependencies:
  - python=3.9
  - numpy
  - pandas
  - sympy
  - tqdm
  - pysr
  - matplotlib
  - scipy
  - pytorch # required for NN in pysr_regimes.py
  - seaborn